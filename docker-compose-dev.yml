services:
  news-scraper:
    build: .
    container_name: news-scraper-dev
    restart: "no"
    ports:
      - "8088:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    shm_size: "512m"
    volumes:
      - ./data:/app/data

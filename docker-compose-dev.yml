services:
  news-scraper:
    build: .
    container_name: news-scraper-dev
    restart: "no"
    ports:
      - "${APP_PORT:-8088}:${APP_PORT:-8088}"   # host==container, entrambi da APP_PORT
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - APP_PORT=${APP_PORT:-8088}
    shm_size: "512m"
    volumes:
      - ./data:/app/data
      - ./debug:/app/debug
